{
  "name": "chatbox",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf ./auth_data && redis-server --daemonize yes && bun run src/index.ts",
    "dev": "rm -rf ./auth_data qrcode.png && redis-server --daemonize yes && bun run --watch src/index.ts"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.5",
    "elysia": "latest",
    "ioredis": "^5.6.1",
    "puppeteer": "^24.10.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.30.0"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.2",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
