{
  "name": "chatbox",
  "version": "1.0.50",
  "scripts": {
    "build": "bun build src/index.ts --outfile ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf ./auth_data && redis-server --daemonize yes && bun run src/index.ts",
    "dev": "PORT=5000 rm -rf ./auth_data qrcode.png && bun run --watch src/index.ts"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.5",
    "elysia": "latest",
    "ioredis": "^5.6.1",
    "puppeteer": "^24.10.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.30.0"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.2",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
